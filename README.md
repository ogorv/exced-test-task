Add user
```
 php artisan app:add-user {login} {password}
```

Deposit
```
 php artisan app:change-balance {login} {amount} deposit {description}
```

Withdraw
```
 php artisan app:change-balance {login} {amount} withdraw {description}
```

Run the development server
```
composer run dev
```
